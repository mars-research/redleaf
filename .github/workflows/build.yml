# xbuild with all possible combinations of flags
name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-18.04

    container: gitlet/redleaf_build:0.2

    strategy:
      matrix:
        debug: ["true", "false"]
        ixgbe: ["true", "false"]
        large_mem: ["true", "false"]

    steps:
    - uses: actions/checkout@v2
    - name: Build DEBUG=${{ matrix.debug }} IXGBE=${{ matrix.ixgbe }} LARGE_MEM=${{ matrix.large_mem }}
      run:  make DEBUG=${{ matrix.debug }} IXGBE=${{ matrix.ixgbe }} LARGE_MEM=${{ matrix.large_mem }}
