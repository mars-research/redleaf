[alias]
rbuild = "build --out-dir=build -Z build-std=core,alloc -Z features=host_dep -Z unstable-options -Z package-features"
rb = "rbuild"
rcheck = "check -Z build-std=core,alloc -Z features=host_dep -Z package-features"
rc = "rcheck"

[build]
target = "x86_64-unknown-redleaf.json"
rustflags = "-Z emit-stack-sizes"
